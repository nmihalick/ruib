epic-start-design

current story:
- as a user, i want a design already there to start with

next story:

tasks:

- verify the reality map
- get questions answered
- finish the stories
+ setup the start page
+ get responsive bootstrap
+ prompt if they would like to start a new design
+ hide the prompt when the start button is clicked
+ figure out how to determine if a design is started
+ set something to identify the design as started
- create a design to start with when prompt is hit
- the UI should show the base template as the created element
